name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v1.0.0)"
        required: true
      prerelease:
        description: "Mark as pre-release"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build signed APK and publish release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "21"
          cache: "gradle"

      - name: Decode signing keystore
        run: echo "${{ secrets.KEYSTORE_FILE }}" | base64 -d > "${{ runner.temp }}/nomixer-release.jks"

      - name: Build signed release APK
        env:
          NOMIXER_KEYSTORE_PATH: ${{ runner.temp }}/nomixer-release.jks
          NOMIXER_KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          NOMIXER_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          NOMIXER_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease --stacktrace

      - name: Collect artifact
        run: |
          mkdir -p out
          apk=$(find app/build/outputs/apk/release -name "*.apk" | head -n1)
          cp "$apk" "out/nomixer-${{ github.event.inputs.tag }}.apk"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: "NoMixer ${{ github.event.inputs.tag }}"
          body: |
            NoMixer build, signed with the project's release key so it can be installed over previous signed releases as a normal update.

            Requires Android 13+ and [Shizuku](https://shizuku.rikka.app/).
          prerelease: ${{ inputs.prerelease }}
          files: out/nomixer-${{ github.event.inputs.tag }}.apk
